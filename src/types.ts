export interface BlogPost {
    id: number;
    title: string;
    body: string;
    authorName: string;
    authorEmail: string;
}

export interface User {
    id: number;
    name: string;
    email: string;
}
